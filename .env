# Discord Bot Token
BOT_TOKEN=your_discord_bot_token_here

# Database Configuration
# For Railway: Only DATABASE_URL is needed (automatically provided)
# For local development: Use PostgreSQL connection string
DATABASE_URL=postgresql://username:password@localhost:5432/ticketbot

# Alternative JDBC format for local development:
# DATABASE_URL=jdbc:postgresql://localhost:5432/ticketbot
# (Note: if using JDBC format, you'll need to handle username/password separately)